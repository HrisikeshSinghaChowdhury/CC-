#include<stdio.h>
#include<conio.h>
void main()
{

int m[10],data,n,i,flag=0,k=0;
clrscr();
printf("enter the size of array");
scanf("%d",&n);
for(i=0;i<n;i++)
{
scanf("%d",&m[i]);
}
printf("enter the element to be searched");
scanf("%d",&data);
for(i=0;i<n;i++)
{
if(m[i]==data)
{
flag=flag+1;
k=i+1;
}
}
if(flag==0)
{
printf("not found");
}
else
{
printf("found in %d",k);
}
getch();
}
