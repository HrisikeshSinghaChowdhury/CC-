#include<stdio.h>
#include<conio.h>
void main()
{
   long int n,temp,rev=0,rem;
   clrscr();
   printf("\n enter a non negeative integer :");
   scanf("%ld",&n);
   temp=n;
   while(temp>0)
      {
      rem=temp%10;
      rev=rev*10+rem;
      temp=temp/10;
      }
   printf(" \n the sum of the digits of the number %ld is = %ld",n,rev);
   getch();
}
