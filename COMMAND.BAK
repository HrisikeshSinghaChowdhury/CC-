#include<stdio.h>
#include<conio.h>
void main(int argc,char *argv[])
{
   int i,x,y;
   clrscr();
   printf("\n No.of arguments =%d",argc);
   printf("\n List of arguments");
   for(i=1;i<argc;i++)
   {
      printf("\n%s",argv[i]);
   }
   x=atoi(argv[1]);
   y=atoi(argv[2]);
   printf("\n%d %d",x,y);
   getch();
 }